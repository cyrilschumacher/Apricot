<Page
    x:Class="Apricot.Views.VarietyInformationPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:sampleData="using:Apricot.Shared.SampleData"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    DataContext="{Binding VarietyInformation, Source={StaticResource Locator}}"
    mc:Ignorable="d" d:DataContext="{d:DesignInstance IsDesignTimeCreatable=True, Type=sampleData:VarietyInformationSampleData}">

    <StackPanel>
        <TextBlock Text="{Binding Model.Information.Name}"/>
        <TextBlock Text="{Binding Model.Information.MinimumHumidity}"/>
        <TextBlock Text="{Binding Model.Information.MaximumHumidity}"/>
        <TextBlock Text="{Binding Model.Information.MinimumTemperature}"/>
        <TextBlock Text="{Binding Model.Information.MaximumTemperature}"/>
    </StackPanel>
</Page>
